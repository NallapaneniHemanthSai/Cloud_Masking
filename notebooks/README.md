# Notebooks

**Supplementary only.** Per the project charter, notebooks never constitute deliverables or replace
engineering work — all real logic lives in `backend/app/` modules and `scripts/`. This directory is for
optional exploratory analysis and figure generation that references the engineered code, not for
implementing pipeline logic.
