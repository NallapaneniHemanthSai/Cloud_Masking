{
  "name": "cloud-masking-frontend",
  "private": true,
  "version": "0.2.0",
  "description": "PLACEHOLDER (Milestone 2 scaffold) — React + TypeScript + Vite frontend for the Cloud Masking capstone. Dependencies are declared but NOT installed at this milestone; the real Vite app is scaffolded in Milestone 14.",
  "type": "module",
  "scripts": {
    "//": "Scripts are placeholders until Milestone 14 (Frontend).",
    "dev": "echo \"Frontend is scaffold-only at Milestone 2; implemented in Milestone 14.\" && exit 0",
    "build": "echo \"Frontend build is implemented in Milestone 14.\" && exit 0",
    "lint": "echo \"Lint config added in Milestone 14.\" && exit 0",
    "test": "echo \"Frontend tests added in Milestone 14/16.\" && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "axios": "^1.7.9",
    "leaflet": "^1.9.4",
    "react-leaflet": "^4.2.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/leaflet": "^1.9.14",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
