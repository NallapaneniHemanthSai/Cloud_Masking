# Docker

**Milestone 2: placeholders only.** These files establish the deployment shape; the functional images
and Compose wiring are authored in **Milestone 17 (Docker)** — after the API (M13) and frontend (M14).

| File | Status | Milestone |
|------|--------|-----------|
| `backend.Dockerfile` | placeholder (Python 3.11 base) | M17 |
| `frontend.Dockerfile` | placeholder (Node 20 base) | M17 |
| `docker-compose.yml` | placeholder (`placeholder` profile) | M17 |

Do **not** build or run these at Milestone 2. GDAL/rasterio system dependencies (Risk R-12) are handled
explicitly in the Milestone 17 backend image.
