# Cloud Masking backend — AUTHORITATIVE runtime dependency source (hand-edited).
#
# This is the SOURCE of truth for runtime dependencies. It uses soft constraints (lower bounds / caps),
# NOT exact pins. A fully-pinned lock file (`requirements.txt`) is DELIBERATELY NOT PRESENT yet:
# dependency LOCKING is deferred until package selection stabilises in later milestones (M3+), then
# generated reproducibly, e.g.:
#
#     pip-compile requirements.in -o requirements.txt        # runtime lock
#     pip-compile requirements-dev.in -o requirements-dev.txt  # dev lock
#
# Milestone 2: nothing is installed. Targets Python 3.11.x on Apple Silicon (ADR-0004).
# NOTE: GDAL is a SYSTEM dependency; `rasterio` wheels bundle it (documented in docs/ for M3).

# --- Web / API ---------------------------------------------------------------------------------
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
python-multipart>=0.0.20
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0

# --- Persistence -------------------------------------------------------------------------------
SQLAlchemy>=2.0,<3.0

# --- Deep learning (Apple-Silicon / MPS compatible) --------------------------------------------
torch>=2.4,<3.0
torchvision>=0.19,<1.0
torchmetrics>=1.6,<2.0

# --- Numerics / data ---------------------------------------------------------------------------
numpy>=1.26,<2.0          # <2.0 for broad geo-stack compatibility at this stage
pandas>=2.2,<3.0

# --- Geospatial / imaging ----------------------------------------------------------------------
rasterio>=1.4,<2.0
opencv-python-headless>=4.10,<5.0
albumentations>=1.4,<2.0
Pillow>=11.0,<12.0
scikit-image>=0.24,<1.0
scikit-learn>=1.5,<2.0

# --- Experiment tracking / plotting ------------------------------------------------------------
mlflow>=2.19,<3.0
matplotlib>=3.9,<4.0

# --- Utilities ---------------------------------------------------------------------------------
PyYAML>=6.0,<7.0
tqdm>=4.67,<5.0
requests>=2.32,<3.0
