"""Core cross-cutting concerns: configuration, constants, logging, exceptions.

Milestone 2: skeletons only. The domain (models, services) depends on ``core``; ``core`` depends on
nothing outside the standard library, preserving the clean-architecture dependency direction.
"""
