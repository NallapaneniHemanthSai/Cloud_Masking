"""API layer (FastAPI routers) — SCAFFOLD.

Endpoints (/train /predict /evaluate /models /history /upload /metrics /version /docs) are implemented
in Milestone 13. This layer is an adapter over ``services`` and must contain no domain logic.
"""
