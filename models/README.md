# Models (checkpoints)

Trained model weights/checkpoints are written here. **Contents are git-ignored** (only `.gitkeep` and this
README are tracked). Location is configured via `MODELS_DIR`. Populated from Milestone 6 onward; the model
registry and version metadata are persisted in the database (M13).
